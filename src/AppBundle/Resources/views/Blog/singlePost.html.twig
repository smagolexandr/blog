{% extends 'AppBundle:Blog:layout.html.twig' %}

{% block main_column %}
    <img src="{{ post.image }}">
    <h3>
        {{ post.title }}
    </h3>
    <span>
        {{ post.content }}
    </span>
{% endblock %}

